track the usage using index.
sort the nums;
as they are sorted, two same number can only change place and try to create another combination (which is eventually duplicate).
we dont let them change their palces.